//
//  SwiftUIView.swift
//  Muun
//
//  Created by Fernando Rivera Castillo on 24/11/23.
//

/*import SwiftUI

struct Cardtask: View {
    let task: Task
    var body: some View {
        
}

struct Cardtask_Previews: PreviewProvider {
    static var previews: some View {
        // Crear una instancia de TaskViewModel
        let viewModel = TaskViewModel()

        // Asegúrate de que storedTasks no está vacío
        if !viewModel.storedTasks.isEmpty {
            // Pasar el primer Task a Cardtask
            Cardtask(task: viewModel.storedTasks[0])
        } else {
            Text("No hay tareas disponibles")
        }
    }
}
*/
